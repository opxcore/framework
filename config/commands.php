<?php

return [
    // List of commands for application. Each command must be keyed by its signature
    // and value must be class name of command implementing CommandInterface.
    // If configuration caching is enabled all commands list would be cached also.
];